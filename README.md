# TableCRM Mobile - Мобильная форма создания заказа

Мобильное веб-приложение для создания заказов в системе TableCRM.

## 🚀 Возможности

- 🔐 Авторизация по токену
- 👤 Поиск клиента по телефону
- 📦 Выбор товаров с поиском
- 🛒 Корзина с управлением количеством
- 📝 Создание заказа (черновик или проведенный)
- 💾 Сохранение состояния в LocalStorage

## 🛠 Технологии

- **React 19** + **TypeScript**
- **Vite** - сборщик проекта
- **Tailwind CSS** - стилизация
- **Zustand** - управление состоянием
- **Vercel** - деплой

## 📦 Установка

```bash
npm install
```

## 🏃 Запуск

### Разработка
```bash
npm run dev
```

### Сборка
```bash
npm run build
```

### Превью продакшн сборки
```bash
npm run preview
```

## 📱 Использование

1. **Авторизация**: Введите токен авторизации для доступа к системе TableCRM
2. **Поиск клиента**: Введите номер телефона и нажмите "Найти"
3. **Заполнение формы**:
   - Выберите счёт (тип продажи)
   - Выберите организацию
   - Выберите склад
   - Выберите тип цены
4. **Добавление товаров**: Нажмите "Выбрать товары", введите название в поиск и добавьте нужные товары
5. **Создание заказа**:
   - **"Создать продажу"** - создаёт черновик заказа (`status: false`)
   - **"Создать и провести"** - создаёт и проводит заказ (`status: true`)

## 🔌 API Endpoints

Приложение использует следующие API endpoints TableCRM:

- `GET /api/v1/docs_sales/?token=xxx` - проверка токена
- `GET /api/v1/clients?phone=xxx&token=xxx` - поиск клиента
- `GET /api/v1/products?search=xxx&token=xxx` - поиск товаров
- `POST /api/v1/docs_sales/?token=xxx` - создание заказа

## 📄 Структура проекта

```
src/
├── components/
│   ├── AuthForm.tsx      # Форма авторизации
│   ├── OrderForm.tsx     # Основная форма создания заказа
│   └── SelectModal.tsx   # Модальное окно выбора
├── store/
│   └── store.ts          # Zustand store для состояния
├── utils/
│   └── api.ts            # API функции
└── App.tsx               # Главный компонент
```

## 🚢 Деплой

Проект настроен для деплоя на Vercel. Конфигурация находится в `vercel.json`.

## 📝 Формат данных заказа

```json
{
  "organization": 207,
  "warehouse": 39,
  "goods": [
    {
      "nomenclature": 1,
      "quantity": 1,
      "price": 1000
    }
  ],
  "status": true,
  "contragent": 123,
  "contragent_name": "Иван Иванов"
}
```

## 🔒 Безопасность

Токен авторизации сохраняется в LocalStorage и используется для всех API запросов.
